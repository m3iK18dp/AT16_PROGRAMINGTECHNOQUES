1. Tổng quan về LinkedList
	Trong linkedList gồm 2 thành phần:
	+ Chứa 1 con trỏ (Tham chiếu tới object (node) bên cạnh (Phóa bên phải))
	+ Chứa data
2. Về mặt hiệu năng
	+ LinkedList dùng dymanimic memory..
	+ Tùy từng phương thức mà linkedlist sẽ nhanh hoặc chậm hơn so với arraylist
3. 
